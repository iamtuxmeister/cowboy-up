[
    {test_app, [
        {http, [{port, 8080}]},

        {db_backend, sqlite},

        {sqlite, [
            {path, "priv/db/test_app.db"}
        ]},

        %% PostgreSQL — set db_backend = postgres and fill in to activate
        %% {postgres, [
        %%     {host,      "localhost"},
        %%     {port,      5432},
        %%     {database,  "test_app"},
        %%     {user,      "test_app"},
        %%     {password,  "secret"},
        %%     {pool_size, 5}
        %% ]},

        {session_secret, <<"CHANGE_ME_32_BYTES_OF_RANDOM_KEY">>}
    ]},

    {kernel, [{logger_level, info}]}
].
