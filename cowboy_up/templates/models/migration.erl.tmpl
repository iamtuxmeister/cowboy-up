%%% Migration: $version
%%% Generated by cowboy-up.
%%%
%%% This module is discovered automatically by ${app_name}_db at startup.
%%% Do not rename this file — the module name is used for code loading.
-module('$version').

%% Registers this module as a migration for $app_name.
%% Discovered by ${app_name}_db:discover_migrations/0 at startup.
-migration({$app_name}).

-export([version/0, up/0]).

version() -> "$version".

up() ->
    $sql.
