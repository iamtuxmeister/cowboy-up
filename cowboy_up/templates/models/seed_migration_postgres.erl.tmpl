%%% Initial migration — creates the example table.
%%% Generated by cowboy-up new.
-module('${datestamp}_001_create_example').

-migration($app_name).

-export([version/0, up/0]).

version() -> "${datestamp}_001_create_example".

up() ->
    "CREATE TABLE IF NOT EXISTS example ("
    "  id         SERIAL PRIMARY KEY,"
    "  name       TEXT NOT NULL,"
    "  created_at TIMESTAMPTZ DEFAULT NOW()"
    ");".
