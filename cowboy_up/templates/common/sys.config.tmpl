[
    {$app_name, [
        {http, [{port, 8080}]},

        {db_backend, $db_backend_atom},

        {sqlite, [
            {path, "priv/db/${app_name}.db"}
        ]},

        %% PostgreSQL — set db_backend = postgres and fill in to activate
        %% {postgres, [
        %%     {host,      "localhost"},
        %%     {port,      5432},
        %%     {database,  "$app_name"},
        %%     {user,      "$app_name"},
        %%     {password,  "secret"},
        %%     {pool_size, 5}
        %% ]},

        {session_secret, <<"CHANGE_ME_32_BYTES_OF_RANDOM_KEY">>}
    ]},

    {kernel, [{logger_level, info}]}
].
