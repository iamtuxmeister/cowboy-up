/* $display_name — Pico CSS overrides */
/* Pico handles almost everything; add project-specific tweaks here */

nav a[aria-current="page"] {
    font-weight: 600;
}

.empty-state {
    color: var(--pico-muted-color);
    font-style: italic;
}
